[pytest]
testpaths = tests
pythonpath = .
addopts =
    -v
    --tb=short
    --strict-markers
    --timeout=120
timeout_method = thread
markers =
    unit: pure unit tests, no external dependencies
    integration: tests requiring real cameras or services
    slow: long-running tests (5s+ per test)
